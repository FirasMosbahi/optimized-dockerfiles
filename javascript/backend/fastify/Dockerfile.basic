# Dockerfile.basic (non-optimized)
FROM node:20

WORKDIR /app

# Copy everything
COPY . .

# Install ALL dependencies
RUN npm install

# Set environment
ENV NODE_ENV=development
ENV PORT=3000

# Expose port
EXPOSE 3000

# Run with node --watch for development
CMD ["node", "--watch", "src/index.js"]
